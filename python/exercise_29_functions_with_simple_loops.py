#program that contains two different functions

#generating the two list
list_1 = list(range(30))
list_2 = list(range(20))

#the first function should take a list as an argument and return the sum of the element by a loop
#creating the sum function

def = sum_up(userlist):

#the function now taking the list 1 as an argument
list_1 = list(range(30))
def = sum_up(list_1):
      result = 0
      for i in list_1:
          result = result + 1
      return result
          print(list_1)
